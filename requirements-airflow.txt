# Airflow-compatible requirements
# Core ML libraries (versions compatible with Airflow 2.7.0)
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.10.0
imbalanced-learn>=0.11.0
joblib>=1.3.0
xgboost>=2.0.0

# MLflow (compatible version)
mlflow>=2.8.0

# Database
boto3>=1.28.0
psycopg2-binary>=2.9.0

# Kafka
kafka-python>=2.0.2

# Monitoring & Observability
prometheus-client>=0.19.0
psutil>=5.9.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Note: Flask and SQLAlchemy are provided by apache-airflow
# Do not specify versions that conflict with Airflow's dependencies
