{
  "family": "kafka-inference",
  "taskRoleArn": "arn:aws:iam::${AWS_ACCOUNT_ID}:role/${PROJECT_NAME}-task-role",
  "executionRoleArn": "arn:aws:iam::${AWS_ACCOUNT_ID}:role/${PROJECT_NAME}-execution-role",
  "networkMode": "awsvpc",
  "containerDefinitions": []
}
