# Kafka inference service
FROM python:3.10-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY kafka/inference_service.py .
CMD ["python", "inference_service.py"]
